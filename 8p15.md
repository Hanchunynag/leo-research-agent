# LEO 论文本地 RAG 项目面试口述稿

我做的是一个面向 LEO（Low Earth Orbit，低地球轨道）卫星论文的本地 RAG（Retrieval-Augmented Generation，检索增强生成）系统。这个项目的重点不是把 PDF（Portable Document Format，一种版式固定的文档格式）转成文本后接一个向量库，而是把一份结构复杂、可能含有扫描页、双栏排版、公式、表格和图片的学术论文，变成一组可检索、可验证、可追溯的证据。

我把问题拆成四个层次：第一层是解析，即从 PDF 恢复文本、版面和公式表格；第二层是标准化，即把不同解析结果统一为 Canonical JSON（规范化 JSON，中间数据契约）；第三层是知识组织，即恢复章节树并生成结构化 Chunk（检索文本块）；第四层才是检索和生成。这样的拆分是为了避免把“PDF 解析错误”“章节归属错误”“切块错误”和“召回排序错误”混成一个黑盒问题。

整体链路如下：

```text
PDF
  ↓
SHA-256 身份管理与预检查
  ↓
MinerU 主解析
  ↓
Canonical JSON 标准化
  ↓
Parsing Quality Validator（解析质量校验器）
  ↓
表格/公式局部 PaddleOCR fallback（备用恢复）
  ↓
Structure（章节结构树）
  ↓
结构化 Chunk
  ↓
BM25 + BGE-M3 Dense（稠密向量）召回
  ↓
RRF（倒数排名融合）
  ↓
Cross-Encoder（交叉编码器）精排
  ↓
带页码和 block_id 的证据包
  ↓
答案生成与引用校验
```

## 一、先确定下游真正需要什么，而不是先决定用哪个解析器

如果下游只是全文搜索，一个字符串列表就够了；但论文 RAG 需要保留更多身份信息。一个公式不能只保存成一行 LaTeX（数学排版语言），还应该知道它来自哪篇论文、哪一页、原始裁图在哪里、它属于哪个章节、对应哪个原始解析 block（版面内容块），以及这个 LaTeX 是 MinerU 原始结果还是 fallback 恢复结果。表格也不能只保存 OCR（Optical Character Recognition，光学字符识别）后的纯文本，因为行列关系、表头和原始表格图片仍然需要审计。

所以我没有把 Markdown（轻量标记文本）当作系统唯一中间格式。Markdown 适合阅读和展示，但图片链接本身不能表达“这是一个公式 block”“这是一个表格 block”“它的页码是多少”。项目的下游唯一入口是 `data/canonical/<paper_id>/paper.json`，每个 block 大致包含：

```json
{
  "block_id": "P_xxx_p003_b008",
  "paper_id": "P_xxx",
  "page_number": 3,
  "reading_order": 8,
  "type": "equation",
  "bbox": [0, 0, 100, 100],
  "text": "...",
  "latex": "...",
  "image_path": "...",
  "quality": {},
  "raw_block": {}
}
```

`block_id` 是稳定身份，`page_number` 和 `bbox` 用于回到 PDF，`reading_order` 用于恢复阅读顺序，`type` 用于区分正文、公式、表格、图片和算法，`quality` 用于记录校验和恢复状态，`raw_block` 则保留 MinerU 原始内容。之后的 Structure、Chunk、BM25 索引和 Dense 索引都只引用这些身份，不复制一套无法追溯的文本。

## 二、为什么选 MinerU，而不是 Marker、PyMuPDF 或普通 OCR

我选择 MinerU（面向学术文档的版面和内容解析工具）作为主解析器，不是因为它在所有 PDF 上都必然比其他工具准确，而是因为它和当前项目的输入输出契约最匹配。项目需要同时获得阅读顺序、标题候选、正文、公式区域、表格区域、图片裁图、caption（图表说明文字）、bbox（边界框坐标）和原始中间 JSON。当前 MinerU 产物中有 `content_list_v2.json`、`middle.json`、Markdown、公式/表格/图片裁图以及版面信息，适配器可以按照 block 类型和阅读顺序转换成 Canonical block。

这里需要明确一个工程边界：我没有在同一批论文上对 MinerU 和 Marker 做严格 benchmark（基准测试），因此不能在面试中说“MinerU 在所有科学论文上都优于 Marker”。更准确的说法是：在当前项目里，MinerU 已经提供了下游所需的版面资产和 block 级信息，所以选择它作为主解析器；如果未来要证明模型级准确率优势，需要拿相同 PDF、相同人工标注和相同指标做独立对比实验。

Marker（偏向把 PDF 转换成 Markdown 的文档转换工具）可以作为很有价值的替代方案，尤其适合作为阅读型 Markdown 生成器或离线转换器。但当前项目的核心契约不是“得到一篇看起来完整的 Markdown”，而是“得到带身份、页码、类型、坐标和原始资产的 block 集合”。如果直接采用 Marker，就需要额外确认它能否稳定输出并保留这些字段，并重新实现公式、表格和图片资产与 Canonical block 的映射。当前没有这套同数据集验证，所以没有把 Marker 并行接入生产链路。

PyMuPDF（Python 的 PDF 页面和对象访问库）和 pdfplumber（基于 PDF 文本和几何对象的提取库）在项目中仍然有价值，但职责不同。它们适合做 PDF 预检查，例如统计页数、文件大小、文字层是否存在、是否可能是扫描件、是否存在双栏和嵌入图片；也适合在已知坐标时提取文本或做调试。可是仅靠 PDF 文本层和几何对象，不能稳定承担论文级的标题层级恢复、公式语义识别、表格结构恢复和图文关系判断。它们是轻量预检查/底层访问工具，不是当前的完整论文解析器。

普通 OCR，例如 Tesseract 或只做文字识别的 PaddleOCR，主要解决“图像中的字符是什么”，并不自动解决“这些字符属于标题、正文、公式、表格还是 caption”。对整篇双栏论文直接做普通 OCR，还要自己处理阅读顺序、栏间跳转、表格行列和公式结构，因此我没有让普通 OCR 取代 MinerU。PaddleOCR 在本项目中的定位是局部 fallback：当已经知道某个区域是表格或公式时，再对这个区域做专门恢复。

因此当前的选型不是“MinerU 绝对正确”，而是：

```text
MinerU：负责论文级版面和内容主解析
PyMuPDF：负责预检查和 PDF 底层访问
PaddleOCR/PaddleOCR-VL：负责明确目标区域的局部恢复
Canonical JSON：负责把结果变成稳定的数据契约
```

这种组合的主要收益是故障隔离。主解析器有问题时，不需要整篇论文重新走另一套解析器；只对已定位的高风险 block 做恢复，同时保留 MinerU 原始结果，后续可以比较两种结果。

## 三、为什么 MinerU 成功运行不等于内容正确

解析器的进程成功退出只能说明模型和文件处理流程没有崩溃，不等于字符、公式和结构全部正确。项目真实数据中出现过 `\x03`、`\x15` 这样的控制字符，也出现过 `diferent`、`sufer`、`orbi propagators`、`poo estimability` 等疑似字符层或 OCR 污染。它们可能来自 PDF 自身的字体编码，也可能来自解析器或 OCR，单凭结果文本不能武断地归因。

所以 Canonical JSON 后面增加了 Quality Validator（质量校验器）。它在 Structure 和 Chunk 之前检查非法控制字符、异常 Unicode（统一字符编码）、替换字符、公式环境和分隔符是否匹配、空 Figure（图像 block）和空 Table（表格 block），并记录文本字符覆盖率等质量信息。清洗后的字段供下游检索使用，`raw_block` 和质量报告供审计使用；这两者不能混在一起，否则为了让检索干净而丢失问题现场。

质量校验不是内容真值证明。它能发现 `\x03`，但不能证明 `N_1` 没有被误识别成 `N_2`；它能发现 `\begin{array}` 没有闭合，但不能证明矩阵每一列都和原图一致。因此我把质量系统定位为“风险门控和可观测性”，而不是声称自动完成学术级校对。

## 四、表格和公式为什么采用局部容错，而不是重做整篇论文

表格当前严格实现情况 A：MinerU 已经识别出 `table` block，但 `table_html`（表格 HTML 结构）为空或不可用，同时存在表格裁图。此时才通过 `.venv-paddleocr` 中的 PaddleOCR 表格 API 对该图片恢复 HTML，并回填同一个 Canonical table block。MinerU 完全漏检的表格、没有被标记为 table 的算法流程三线表，目前不在这个 fallback 范围内；我没有偷偷把范围扩大成整页表格检测，因为那会引入新的候选区域检测问题。

公式链路只保留两级规则，不引入 Qwen。第一层是 Hard Error Rule（硬错误规则）：检查 LaTeX 是否为空、控制字符是否存在、`\begin`/`\end` 环境是否匹配、花括号是否严重不平衡、`\left`/`\right` 是否明显不匹配，以及公式是否像被截断。第二层是 High-Risk Formula Rule（高风险公式规则）：矩阵或 array、多行公式、多个分式、积分/求和、大量上下标、超长 LaTeX 或特别宽的公式图片命中时，认为它值得二次识别。

```python
if hard_error(mineru_latex):
    use_paddleocr_vl()
elif high_risk_formula(mineru_latex, image):
    use_paddleocr_vl()
else:
    use_mineru()
```

这里的目标不是发现所有数学错误，而是在成本可控的情况下优先阻断明显坏公式和复杂公式风险。普通公式直接采用 MinerU；高风险公式调用 PaddleOCR-VL（带视觉输入的语言模型公式识别流程）。调用通过专用 Python worker（工作进程）中的 API 完成，不使用 CLI（命令行接口），并且同一篇论文由一个 worker 加载一次模型后循环处理多个公式，避免每个公式都重复初始化 0.9B 模型。

恢复结果不覆盖原值，而是保留 `latex_mineru`、`latex_paddleocr_vl` 和最终 `latex`。如果 PaddleOCR-VL 失败，保留 MinerU LaTeX 并在报告中记录失败。这样可以做到 fail-soft（失败时尽量保留可用结果），也可以在以后重新评估规则，而不需要重新运行 MinerU。

## 五、为什么要拆成 Canonical、Structure 和 Chunk 三层

Canonical block 解决“每一块是什么以及来自哪里”；Structure 解决“每一块属于哪个章节”；Chunk 解决“检索时一次拿多大的证据”。如果直接从 Markdown 切 token，会把这三个问题混为一个字符串切分问题。

Structure（章节结构树）按照 MinerU 的阅读顺序处理标题候选，综合标题 block、标题编号、版面位置和当前父标题，维护一个标题栈。例如读到 `III. LEO SV Tracking` 时创建一级章节，读到 `A. Process Noise Covariance Determination` 时创建二级章节，后面的正文、公式、表格和图片挂到最近的章节节点。它不是重新理解论文，而是把已存在的标题线索组织成确定性的父子关系；如果 MinerU 把一个普通文字块错误标成标题，Structure 也可能受到影响，所以标题纠正和质量报告仍然重要。

Chunker 不跨章节随意拼接。每个 Chunk 保存 `work_id`、`document_id`、兼容的 `paper_id`、`section_path`、页码范围、`block_ids`、内容类型、父章节上下文和 overlap context（重叠上下文）。因此检索结果可以回答“命中了哪篇论文的哪个章节、哪一页、哪些 block”，而不是只能返回一段没有来源的文本。

Figure 的处理也在这层完成。没有 caption 的空 Figure 不进入 `retrieval_text`（检索文本），但保留 block_id 和图片路径；有 caption 的 Figure 以 `[Figure] + caption` 进入检索。表格则保留 `table_html`、表格文本和原始图片路径。这样不会让大量孤立的 `[Figure]` 进入 embedding（向量表示）并污染语义空间，同时还保留后续做视觉摘要或精确表格问答的可能性。

## 六、为什么 Chunk 最大值先定为 700 token，以及目前能证明什么

当前默认配置是：`maximum_tokens=700`、`minimum_chunk_tokens=80`、`overlap_tokens=80`。700 token（模型分词后的长度单位）不是某个理论公式推导出的最优值，也不是做完参数搜索后的结论，而是当前的工程基线（用于后续比较的初始配置）。选择它时考虑了三点：论文中的一个定义、解释段落和相邻公式通常需要一起出现；Chunk 太短会把定义和解释拆开；Chunk 太长会让一个检索结果混入多个主题并增加上下文消耗。

代码会优先保持章节和 block 边界，只有单个 block 超过上限时才按句子拆分，最后才按词拆分。`minimum_chunk_tokens=80` 用于避免过短的父章节单独成为噪声 Chunk；如果一个父章节只有少量正文且后面紧接子章节，系统可以把它保存为子章节的 `parent_context`，不直接制造一个很小的检索单元。

Overlap 也不是把前一个 Chunk 的最后 80 token 机械复制过来。当前实现只从前一个同章节、同内容区的 paragraph（段落）或 list（列表）中取尾部上下文；不会复制 equation、figure、table 或 algorithm 的尾巴。这样可以避免一个矩阵刚好位于 Chunk 边界时，把矩阵后半截带进下一个本来讨论表格的 Chunk。80 只是上限，实际重叠内容可能小于 80 token，也可能没有 overlap。

关于 700 的评估，我必须如实区分已有证据和缺失证据。当前有 21 道人工标注检索问题、135 个 Chunk，并且已经评估 BM25、BGE-M3 Dense、RRF 和 Reranker 的 Recall@1/5/10（前 1/5/10 个结果中是否命中相关证据）、MRR（平均倒数排名，越早命中越高）和 nDCG@10（前十结果的排序质量指标）。这些结果能说明当前整条检索链路是否可用，但不能证明 700 一定优于 400、500 或 900，因为当前没有固定其他变量、只改变 Chunk 大小的 ablation（消融实验）。

现有整体检索基线如下：

| 指标 | BM25 | BGE-M3 Dense | RRF | Reranker |
|---|---:|---:|---:|---:|
| Recall@1 | 0.3571 | 0.2381 | 0.3810 | **0.5714** |
| Recall@5 | 0.5714 | 0.7381 | **0.8095** | 0.7857 |
| Recall@10 | 0.7857 | 0.8333 | 0.8810 | **0.9286** |
| MRR | 0.5137 | 0.4480 | 0.5409 | **0.7110** |
| nDCG@10 | 0.5685 | 0.5441 | 0.6232 | **0.7416** |

所以目前可以说“700 在现有 21 道问题和当前检索管线下工作”，不能说“700 已经被实验验证为全局最优”。正式的 Chunk 消融应该固定同一批 Canonical JSON、同一 Structure、同一问题集、同一 BM25、同一 BGE-M3 revision（模型版本标识）、同一 RRF `k=60` 和同一 Reranker，只改变 `maximum_tokens`，例如比较 400/500/700/900，同时记录检索指标、Chunk 数量、平均 token 数、索引大小、检索延迟和生成阶段 Context token 消耗。只有这样才能把参数选择从工程折中升级为有数据支撑的结论。

## 七、为什么选择 BGE-M3，以及为什么当前只用它的 Dense 单向量

BGE-M3（BAAI 的多语言文本向量模型）适合当前中英文混合的论文库和问题集。Dense embedding（稠密向量表示）会把一段文本映射成连续数值向量，查询和 Chunk 可以通过向量相似度比较语义接近程度。当前使用 `BAAI/bge-m3`，固定 revision，输出归一化的 1024 维单向量，存入 Qdrant local（本地运行的向量数据库）。

选择 BGE-M3 的工程依据有三点。第一，当前语料包含英文论文、中文提问以及大量技术术语，需要一个统一的多语言表示模型，而不是分别维护中英文两套 embedding。第二，它可以本地运行，数据不需要发送到外部 API（应用程序接口），符合本地论文库的隐私和可复现要求。第三，Sentence-Transformers（通用文本向量推理框架）和 Qdrant local 的组合已经接入现有 `EmbeddingProvider`（向量模型接口），模型 revision、向量维度、距离和 Chunk 指纹都写入 manifest（索引清单），索引不一致时会拒绝复用。

但我不会把 BGE-M3 描述成经过本项目证明的最优 embedding。当前没有和一组其他 embedding 模型做完整同条件 benchmark。真实基线只能说明：在 21 道问题上，BGE-M3 Dense 的 Recall@5 是 0.7381、Recall@10 是 0.8333，能提供有价值的语义召回；但 Recall@1 和 MRR 分别低于 BM25，说明它不是所有问题类型都更好。它的实际价值要结合 BM25 的互补性判断，而不是看一个模型名就下结论。

BGE-M3 本身也支持 sparse retrieval（稀疏检索，输出词项级权重）和 multi-vector（多向量表示，一段文本对应多个向量）等能力，但当前项目没有启用它们，只使用单向量 Dense 路径。这样做是为了控制系统复杂度、索引体积和调试成本；如果未来启用 sparse 或 multi-vector，需要重新设计索引、查询、分数融合和评测，不能把它们和当前单向量结果混为一谈。

替代方案包括 TF-IDF（基于词频和逆文档频率的传统向量检索）、其他本地多语言 embedding、远程 Embedding API、ColBERT（保留多个 token 级向量的多向量检索方法）以及专门的跨语言模型。它们各有适用场景，但当前项目没有足够的同数据集结果证明哪一个一定更好。因此 BGE-M3 的表述应该是“当前可复现、能覆盖中英文、已经接入并有基线数据的工程选择”，而不是“理论上最强的模型”。

## 八、为什么使用 BM25 + BGE-M3，而不是单一路径

BM25（基于词项频率、逆文档频率和文档长度归一化的稀疏检索算法）擅长精确匹配。论文检索中有很多不能被模糊化的字符串，例如 `SGP4`、`EKF`、`J_2`、`Q_emp`、公式编号、变量名和卫星型号。用户输入这些词时，BM25 可以利用词项出现和稀有度给出可解释的结果；Dense 向量如果把两个相近符号映射得过于接近，反而可能损失区分度。

Dense 检索擅长表面词不同但语义接近的情况。例如用户问“闭环星历为什么改善定位性能”，论文可能写成“refined ephemerides improve receiver localization”。两边未必有完全相同的词，但语义表示可能相近。纯 BM25 会受到词面差异影响，纯 Dense 又可能漏掉精确变量名和缩写。

当前评测也显示了这种互补关系：BM25 的 Recall@1 是 0.3571，高于 Dense 的 0.2381；Dense 的 Recall@5 和 Recall@10 是 0.7381 和 0.8333，高于 BM25 的 0.5714 和 0.7857。这说明 BM25 更可能把最精确的结果排在前面，而 Dense 在扩大候选覆盖方面更有帮助。两路各取 Top-20 后用 RRF 融合，RRF（Reciprocal Rank Fusion，按多个结果排名融合而不直接比较原始分数）使用 `k=60`，得到 Recall@5 0.8095、Recall@10 0.8810，优于单路召回。

RRF 的好处是不同检索器的分数不需要处在同一个数值尺度，它只利用名次；但它不能创造证据，如果两路都没有召回正确 Chunk，RRF 也无法修复。当前联合候选池的 Oracle Recall（假设后续排序完美时候选集合包含正确证据的上限）为 0.9286，这个数字很重要：它说明在这 21 道题上，候选池已经覆盖大部分标注证据，但仍有约一部分问题不是简单靠精排就能解决的。

在 RRF Top-20 上再使用 BGE Cross-Encoder（把 query 和候选 Chunk 拼接后联合打分的精排模型），主要目的是改善排序，而不是扩大候选集合。它把 Recall@1 从 RRF 的 0.3810 提高到 0.5714，把 MRR 从 0.5409 提高到 0.7110，把 nDCG@10 从 0.6232 提高到 0.7416；但 Recall@5 从 0.8095 变为 0.7857，略有下降。因此面试时应准确说“精排显著改善首位和总体排序质量，但不是所有指标都单调提升”。CPU 下精排 20 对候选平均约 14.01 秒、P95（95 分位延迟）约 17.18 秒，当前更适合作为离线或低并发模式，而不是未经优化的交互式默认路径。

## 九、当前运行结果怎样解释，不能把什么说过头

最近一次知识构建输出了 7 篇论文、122 个章节、797 个 searchable block（可检索内容块）和 135 个 Chunk。结构和 Chunk 具有输入指纹，因此命令再次运行时会复用没有变化的论文；一次增量构建中是 1 篇 Structure/Chunk 重新构建、6 篇复用，这不是只处理了一篇论文，而是缓存命中后的增量行为。若要强制所有论文重算，需要改变输入指纹、策略版本或显式清理/重建对应产物，不能把“reused”误解成“没有进入全量扫描”。

公式恢复的一次全量运行记录中，7 篇论文共有 167 个公式，其中 156 个命中两级规则并调用 PaddleOCR-VL，156 个成功恢复，失败数为 0。这个耗时约 5 分 18 秒，主要成本来自 CPU 下加载和运行 0.9B 视觉语言模型，而不是 Chunker；因此实际工程中要做缓存、批量 worker 和按风险调用，不能对每个公式重复启动进程。表格恢复仍然只统计情况 A，不应把“表格图片存在”表述成“所有表格都已经还原成结构化 HTML”。

当前测试和检索基线能证明的是：数据管线可重复运行，解析资产可缓存，Canonical、Structure、Chunk、BM25 和 Dense 之间有明确契约，21 道标注问题上混合检索和精排有可量化结果。它们不能证明所有 PDF 都无误、所有公式都数学等价、所有表格都被发现，也不能证明 700 token 或 BGE-M3 是全局最优。我的面试回答会把“已实现”“已测量”和“下一步需要验证”分开。

## 十、我对这个项目的核心思考

我认为 RAG 最容易被忽略的部分不是最后调用哪个大模型，而是进入检索系统的证据是否可信。一个漂亮的向量召回器建立在错误 OCR、断裂公式或错误章节归属之上，最终只会更快地找到错误内容。于是我把原始 PDF、MinerU 原始产物、Canonical block、质量报告、Structure、Chunk、索引 manifest 和检索结果都保留下来，并让它们携带稳定身份。

最终答案应该能够沿着下面的路径反向追溯：

```text
answer
  → evidence/context bundle
  → chunk_id
  → section_path
  → canonical block_id
  → page_number / bbox
  → original PDF 或公式/表格裁图
```

这条链路体现了我对技术选型的判断：MinerU 解决论文版面解析，Canonical JSON 解决数据契约，质量校验解决“解析成功不等于内容正确”，PaddleOCR 只解决已定位区域的局部风险，Structure 和 block-aware Chunk 解决语义边界，BM25 与 BGE-M3 解决互补召回，RRF 和 Cross-Encoder 解决排序，最后的来源身份解决可解释性。下一步最有价值的工作不是盲目增加更多模型，而是完成正文异常词检测、Chunk 参数消融、不同 embedding 的同条件 benchmark，以及引用覆盖和拒答质量评估，让每个工程选择都逐步从“合理假设”变成“有数据支撑的结论”。
