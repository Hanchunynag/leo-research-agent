# LEO 论文本地 RAG 项目面试口述稿

我做的是一个面向 LEO 卫星论文的本地 RAG 系统。这个项目的重点不是简单地把 PDF 转成文本，再建立一个向量库，而是解决学术论文场景下更实际的问题：PDF 格式复杂，公式和表格容易丢失，章节结构可能被破坏，OCR 结果不一定可靠，而且最终检索结果必须能够追溯到原始论文的页码和内容块。

整个系统的流程是：

```text
PDF
  ↓
论文入库与 SHA-256 身份管理
  ↓
MinerU 解析
  ↓
Canonical JSON 标准化
  ↓
质量校验与局部 PaddleOCR fallback
  ↓
章节 Structure 构建
  ↓
结构化 Chunk
  ↓
BM25 + BGE-M3 Dense 检索
  ↓
RRF 融合
  ↓
Cross-Encoder 精排
  ↓
带来源引用的答案生成
```

## 一、为什么不直接把 PDF 转成 Markdown 后切块

Markdown 更适合展示，不适合作为 RAG 的核心中间格式。比如公式在 Markdown 中可能只是一个图片链接，但 RAG 还需要知道它属于哪篇论文、位于哪一页、对应哪个原始 block、图片在哪里、是否经过二次 OCR，以及它属于哪个章节。

因此我在 MinerU 后面增加了 Canonical JSON 层。每个 block 都会被标准化为统一结构：

```json
{
  "block_id": "P_xxx_p003_b008",
  "paper_id": "P_xxx",
  "page_number": 3,
  "reading_order": 8,
  "type": "equation",
  "text": "",
  "latex": "...",
  "image_path": "...",
  "quality": {},
  "raw_block": {}
}
```

这样 Markdown、Chunk 和索引都不再是孤立文本。最终可以沿着 `answer → chunk → section → canonical block → page → original PDF/image` 反向追溯答案来源，这对论文问答比单纯返回相似度更重要。

## 二、为什么选择 MinerU，但又不完全信任 MinerU

我选择 MinerU 作为主解析器，是因为它不只是做 OCR，还会结合 PDF 文本层、页面视觉布局、标题层级、公式区域、表格区域以及图片和 caption 的关系。对于双栏论文、标题、公式、表格和图片，单独使用 PyMuPDF 或普通 OCR 很难恢复完整结构。

但 MinerU 成功运行，不代表内容一定正确。真实数据中曾经出现过 `\x03`、`\x15`、`diferent`、`sufer`、`orbi propagators` 和 `poo estimability`。因此我的设计不是“MinerU 失败才处理”，而是：

```text
MinerU 主解析
  ↓
Canonical 质量校验
  ↓
只对有风险的 block 做局部恢复
```

## 三、为什么拆分三个虚拟环境

项目使用三个环境：`.venv` 运行主项目、Canonical、Chunk、索引和测试；`.venv-mineru` 专门运行 MinerU；`.venv-paddleocr` 专门运行 PaddleOCR 和 PaddleOCR-VL。

这样做是为了隔离依赖。MinerU、PaddleOCR 和 PaddleOCR-VL 的 Python 包、Paddle 版本和模型加载逻辑可能冲突，如果全部放在一个环境中，容易出现包版本覆盖和模型初始化失败。

主程序通过专用环境的 Python worker 调用 PaddleOCR，但 worker 内部调用的是 Python API，不是 `paddleocr doc_parser` CLI。公式 worker 的核心代码是：

```python
from paddleocr import PaddleOCRVL

pipeline = PaddleOCRVL(
    use_doc_orientation_classify=False,
    use_doc_unwarping=False,
    use_layout_detection=False,
    use_ocr_for_image_block=False,
    device="cpu",
)

results = pipeline.predict(
    image_path,
    prompt_label="formula",
    format_block_content=True,
    max_new_tokens=1024,
)
```

同一篇论文的多个公式会被写入 manifest，由一个 worker 加载一次模型后循环处理，避免每个公式重复初始化 0.9B 模型。

## 四、表格为什么只处理情况 A

当前表格恢复严格限定为情况 A：MinerU 已经识别出 `table` block，但是 `table_html` 为空或无效，同时存在表格图片。此时才调用 PaddleOCR TableRecognitionPipelineV2，将生成的 HTML 回填到原来的 table block。

```text
MinerU table block
  ↓
table_html 无效？
  ↓
读取表格图片
  ↓
PaddleOCR TableRecognitionPipelineV2
  ↓
回填 HTML
```

我没有处理 MinerU 完全漏检的表格，也没有扫描整页寻找表格。因为那会扩大系统边界，需要重新解决表格候选区域检测，并区分表格、算法流程、正文和图片。当前先保证已被识别为表格、但 HTML 丢失的情况能够恢复。

## 五、公式为什么采用两级规则

公式链路没有使用 Qwen，也暂时没有引入 PP-FormulaNet，而是使用两级规则加 PaddleOCR-VL fallback。

第一层 Hard Error Rule 检查 LaTeX 是否为空、是否包含控制字符、`\begin` 和 `\end` 是否匹配、括号是否平衡、`\left` 和 `\right` 是否匹配，以及公式是否明显截断。

第二层 High-Risk Rule 判断公式是否复杂到值得重新识别，包括矩阵、多行公式、多个分式、多个积分或求和、大量上下标、超长 LaTeX，以及特别宽的公式图片。

```python
if hard_error(mineru_latex):
    use_paddleocr_vl()
elif high_risk_formula(mineru_latex, image):
    use_paddleocr_vl()
else:
    use_mineru()
```

普通公式例如 `x_k = F x_{k-1} + w_k` 没有必要全部调用 0.9B 模型；但矩阵和复杂推导中容易出现 `N_1` 识别成 `N_2`、上下标丢失、矩阵列错位等问题。这类错误可能不会破坏 LaTeX 语法，所以单纯检查括号是不够的。

恢复结果不会覆盖 MinerU 原始结果，而是保存：

```json
{
  "latex_mineru": "...",
  "latex_paddleocr_vl": "...",
  "latex": "最终采用的结果"
}
```

## 六、为什么要在 Chunker 之前做质量校验

如果 Canonical 输入已经污染，Chunker 再好也只能得到污染后的 Chunk，也就是 Garbage In，Garbage Out。

当前 Quality Validator 会检查控制字符、异常 Unicode、替换字符、LaTeX 环境、公式分隔符、空 Figure、空 Table 和文本字符覆盖率。控制字符会从检索文本中清理，但具体问题会被写入质量报告，完整的 MinerU 数据仍保存在 `raw_block` 中。

因此系统同时保留三类信息：原始解析结果用于审计，清洗后的字段用于 Structure 和检索，质量报告用于定位问题。

## 七、Structure 和 Chunk 是怎么构建的

Canonical block 不会直接进行 token 切分，而是先构建 Structure。Structure 主要依据标题 block、MinerU 标题层级、标题编号、阅读顺序、页面位置和当前父标题构建章节树：

```text
II. MODEL DESCRIPTION
├── A. LEO Satellite Dynamics
└── B. Measurement Models

III. LEO SV Tracking
├── A. Process Noise Covariance Determination
└── B. Ephemeris Closed-loop Tracking
```

每个正文、公式、图片和表格 block 都会挂到最近的章节节点下。Chunk 因此是在章节语义范围内生成的，而不是跨章节机械拼接。每个 Chunk 还保存 `section_path`、页码、`block_ids`、`content_types` 和父级上下文。

当前配置是 `maximum_tokens=700`、`minimum_chunk_tokens=80`、`overlap_tokens=80`。700 token 是上下文完整性和检索粒度之间的折中。Chunk 太短，公式、定义和解释容易拆开；Chunk 太长，一个 Chunk 又可能包含多个无关主题。

Overlap 不是机械复制上一个 Chunk 的最后 80 token。如果上一块以矩阵公式结尾，复制公式尾巴对下一个 Chunk 没有帮助。因此当前 overlap 只从完整 paragraph 或 list 中选择，不复制 equation、figure、table 和 algorithm。

## 八、Figure 和 Table 如何进入检索

没有 caption 的 Figure 不进入 `retrieval_text`，但保留 `block_id` 和图片路径：

```json
{
  "content": "",
  "searchable": false,
  "exclusion_reason": "empty_figure"
}
```

有 caption 的 Figure 会以 `[Figure] + caption` 的形式进入检索。这样可以避免大量空的 `[Figure]` 污染 embedding。

表格目前已经可以进入 BM25 和 Dense 检索，但部分表格被转换成扁平文本。对于普通语义检索已经够用，但如果未来要支持精确的行列查询，还需要同时保留 `table_html`、`table_rows` 和 `table_text`。

## 九、为什么使用 BM25、BGE-M3、RRF 和 Cross-Encoder

我没有只使用向量检索。论文中有大量精确术语和符号，例如 `SGP4`、`EKF`、`Q_emp`、`J_2` 和 `pseudorange`，这些内容用 BM25 检索非常有效。

但用户的问题不一定和论文原句使用相同表达，例如用户问“为什么闭环星历能够改善定位性能”，论文中可能写成“refined ephemerides improve unknown receiver localization”。这种语义相近但表面词不同的问题，Dense 检索更有优势。

因此当前流程是：

```text
BM25 Top-20
+
BGE-M3 Dense Top-20
↓
RRF 融合，k=60
↓
Cross-Encoder 对 Top-20 精排
```

BM25 的优点是本地、便宜、可解释；BGE-M3 适合中英文语义检索；Qdrant local 不依赖外部服务；RRF 不要求 BM25 分数和向量分数处于同一尺度；Cross-Encoder 只对小候选集重排，可以控制推理成本。

最终交给生成模型的不是裸文本，而是带有 `chunk_id`、`paper_id`、`document_id`、`work_id`、`section_path`、页码和 `block_ids` 的 Context Bundle。

## 十、当前实际结果和工程思考

今天对 7 篇论文执行了完整重建。公式总数是 167 个，其中 156 个触发 fallback，156 个恢复成功，失败数为 0。知识层完成了 7 篇 Canonical、7 篇 Structure 和 7 篇 Chunk 重建，共得到 135 个 Chunk、782 个 searchable block，质量报告没有 issues。测试结果是 251 passed、1 skipped。

当前最大的剩余问题不在 Chunker，而在正文 OCR 的语义质量。例如 `diferent`、`sufer`、`exploi`、`poo estimability`、`coeficients` 和 `efects` 等词，不一定是控制字符或结构错误，更像 PDF 字符层或 OCR 造成的语义损坏。

我没有直接自动纠正这些词，因为学术论文中存在很多专业缩写、变量名和领域术语，盲目拼写纠正可能修改正确内容。更合理的下一步是增加正文异常检测器，先标记异常词，再保留原文，必要时进行人工确认或上下文校正。

我对这个项目最大的思考是：RAG 的核心不只是选择哪个 embedding 模型，而是首先保证进入检索系统的证据可信、可解释、可追溯。即使最终答案出现问题，也应该能够沿着 `answer → chunk → section → canonical block → page → original PDF/image` 反向定位问题来源，而不是只得到一个无法解释的相似度结果。
