# 问题记录

> 维护规则：每个问题按“问题 / 解决方案 / 最终效果”记录。新问题持续追加，历史记录不删除；最终效果以实际测试结果为准。

## 记录 001：Docker Compose 在错误目录执行

### 问题：

在不包含 Compose 配置文件的目录执行 `docker compose up -d`，Docker 返回 `no configuration file provided: not found`，学术 Agent 服务无法启动。

### 解决方案：

切换到项目根目录 `/Users/hanchunyang/研究生/本地论文RAG/leo-research-agent` 后再执行 Compose 命令。

### 最终效果：

Compose 能够读取项目的 `docker-compose.yml`，进入环境变量校验和服务启动流程。

## 记录 002：Compose 缺少数据库密码环境变量

### 问题：

`docker compose config -q` 和 `docker compose up -d` 报错：`MYSQL_PASSWORD`、`MYSQL_ROOT_PASSWORD` 未设置。

### 解决方案：

在项目运行环境的 `.env` 中补齐数据库密码配置，并保留 `docker-compose.yml` 对必填变量的校验。

### 最终效果：

Compose 配置可以完成插值校验；数据库、API、Worker、Redis、Qdrant 及前端服务可以使用统一环境配置启动。密码不写入本记录。

## 记录 003：学术 Agent 与旧服务端口绑定不清晰

### 问题：

访问 `http://127.0.0.1:8000/scholar` 时，无法明确判断该页面是否由新的 CrewAI 学术 Agent 提供，容易与旧服务混淆。

### 解决方案：

为学术 Agent 单独使用 `8001` 端口，并通过 Scholar 代理和 `agent.heuhcy2026.top` 指向该服务；旧服务继续保留在 `8000`。

### 最终效果：

学术 Agent 入口为 `http://127.0.0.1:8001/scholar`，生产域名 `https://agent.heuhcy2026.top/scholar` 指向独立的 Scholar 服务，不再与旧服务共用入口。

## 记录 004：真实 Worker 任务被旧任务阻塞

### 问题：

旧 Worker 任务占用队列，导致新的真实 Scholar Run 长时间等待，无法及时完成端到端验证。

### 解决方案：

通过任务取消接口取消旧任务，并重启 Worker 清理运行态阻塞；不删除数据库、论文数据或 Qdrant 数据。

### 最终效果：

队列恢复可消费状态；旧任务 `RUN_59084cc1fd60e546` 已取消并完成清理，后续 Run 可以重新进入 Worker 执行。

## 记录 005：CrewAI 结构化输出合同过大导致 JSON 截断

### 问题：

真实调用远程 Qwen 兼容 API 时，Research Agent 的结构化输出 Schema 和上下文过大，在 2048 tokens 限制下出现 JSON 截断，Run 失败。远程 LLM API 本身连通正常。

### 解决方案：

将结构化调用限制为明确的最小认知合同：Supervisor 只返回状态、路由和最终回答；Research 只返回状态和研究摘要；Writer 只返回状态和是否需要研究；Reviewer 只返回决策。完整证据、DraftPatch、ReviewReport 由领域服务保存和回填；同时压缩传入 CrewAI 的 `domain_result`、`draft_patch`、`evidence` 上下文，并保留无工具执行副本。

### 最终效果：

单元测试和编排测试已通过（`29 passed`）。Worker 镜像已重建并重启；真实 Run `RUN_f7d64922258cc126` 使用 CrewAI + 远程 Qwen 兼容 API 成功完成，Job 为 `SUCCEEDED`，attempt 为 `1`，没有 JSON 截断、失败或重复重试。

## 记录 007：本地 BGE-M3 CPU 检索延迟较高

### 问题：

生产真实 Run 在本地 CPU 执行 BGE-M3 检索时负载较高，15 篇论文、283 个二级 Chunk 的一次 `SUPPORT_CLAIM` Run 约耗时 6 分钟，期间事件游标会在检索阶段较长时间不变化。

### 解决方案：

保留当前本地 BGE-M3 和两阶段 Paper-Level → Per-Paper Content 检索正确性；通过 Worker 进程监控、持久化事件和终态查询区分“慢检索”与“任务失败”，并禁止因等待而重复提交相同 Run。后续可在不改变增量索引语义的前提下单独优化模型量化、批处理或硬件执行策略。

### 最终效果：

真实检索链路最终成功，Run `RUN_f7d64922258cc126` 为 `COMPLETED`，CrewAI 事件 44 条完整回放，SSE 从游标 40 可正常收到 41–44 和 `end` 事件。功能正确性已验证，但生产延迟优化仍属于后续工作。

## 记录 008：写作缺少 LaTeX 稿件时错误不够明确

### 问题：

当项目论文库存在但项目根目录没有 root `.tex` 文件时，`WRITE_CONCLUSION` 会捕获底层 `FileNotFoundError` 并以 `WRITING_FAILED` 结束，用户无法直接知道需要先准备 LaTeX 稿件。

### 解决方案：

在 CrewAI Writer capability 边界专门捕获 `FileNotFoundError`，转换为 `WriterAgentOutput(status="INSUFFICIENT_MANUSCRIPT_STATE")`，由 Flow 继续以结构化错误码结束；不创建、不修改任何 `.tex` / `.bib` 文件。

### 最终效果：

修复已进入 Worker 镜像。生产复测 Run `RUN_61e0f77eb64836b1` 正确以 `INSUFFICIENT_MANUSCRIPT_STATE` 失败，Job 为 `SUCCEEDED`、attempt 为 `1`，事件中没有泛化的 `WRITING_FAILED`，且论文文件未被修改。

## 记录 009：无 Patch 时直接进入 Review

### 问题：

在没有提供 `patch_id` 的情况下调用 `REVIEW`，系统没有可审查的 DraftPatch；如果继续调用 Reviewer，会越过已有 Patch 生命周期边界。

### 解决方案：

保留 Review 入口的前置校验：没有有效 Patch 引用时直接返回 `REVIEW_REQUEST_INVALID`，不调用 Research、不创建 DraftPatch、不执行 Apply。

### 最终效果：

生产 Run `RUN_134d29e692851416` 在路由后安全失败，Job 为 `SUCCEEDED`、attempt 为 `1`，终态事件记录 `REVIEW_REQUEST_INVALID`；`WRITE_ABSTRACT` 的无稿件测试 Run `RUN_9a3d7167033d463a` 同样正确返回 `INSUFFICIENT_MANUSCRIPT_STATE`。

## 记录 006：项目路径名称存在空格差异

### 问题：

终端上下文使用了不存在的路径 `本地论文 RAG`，实际项目目录为 `本地论文RAG`，导致命令无法启动。

### 解决方案：

统一使用实际项目根目录 `/Users/hanchunyang/研究生/本地论文RAG/leo-research-agent`。

### 最终效果：

测试、编译、Git 检查和 Docker Worker 构建均已能在正确项目目录执行。

## 记录 010：Qwen 隐式推理耗尽 CrewAI 结构化输出预算

### 问题：

远程 `qwen3.5:9b` 兼容 API 将模型推理放在 `message.reasoning` 字段。当 CrewAI 的 Research Agent 请求短结构化 JSON 时，模型在 `max_tokens=2048` 内持续生成推理，最终 `message.content` 为空且 `finish_reason=length`，CrewAI 报错 `No JSON found in response`，导致真实 Run 进入 `RESEARCH_FAILED`。

### 解决方案：

在 CrewAI 结构化、无工具调用的认知请求中向 OpenAI-compatible Provider 发送 `reasoning_effort: "none"`，仅关闭这类短 JSON 合同的隐藏推理；普通回答生成和工具调用保持原有配置。Provider 同时保留对旧版测试 Provider 的兼容回退。

### 最终效果：

最小真实请求已返回有效 `ResearchAgentOutput`：`status=COMPLETED`、`research_summary=ok`；相关单元与编排测试为 `33 passed`。修复后真实 Scholar Run `RUN_6bc237a227554527` 以 CrewAI 成功完成，Job 为 `SUCCEEDED`、attempt 为 `1`，并完成证据持久化验证：`3` 条已验证证据、`2` 个 claims、`5` 个 citation bindings、`3` 个 citation requirements。

## 记录 011：Compose Qdrant 服务与应用嵌入式 Qdrant 路径不一致

### 问题：

Docker Compose 启动了 `qdrant/qdrant` 服务，但当前应用的 Dense Retrieval 实际通过 `QdrantClient(path=...)` 使用共享卷中的嵌入式索引：`data/index/qdrant_papers_dense` 与 `data/index/qdrant_dense`。因此 Compose Qdrant 容器本身没有承载当前论文向量，存在部署拓扑与实际数据平面不一致的问题。

### 解决方案：

已完成只读核查并保留现有嵌入式索引，不删除或迁移论文向量，避免破坏已验证数据。后续应由管理员单独安排 Qdrant 远程客户端统一迁移，并同步修改索引构建、检索、健康检查和备份策略；在迁移完成前不宣称 Compose Qdrant 已承载业务向量。

### 最终效果：

当前生产 Run 仍可通过嵌入式 Qdrant 正常检索，索引一致性为 `true`，但 Qdrant 容器数据平面统一工作尚未完成，作为后续部署整改项持续保留。

## 记录 012：学术 Agent 前端无法有效展示任务、历史与 LaTeX 稿件

### 问题：

原 Scholar 页面是一个长页面，顶部“概览 / 运行记录 / 项目 / 审批 / 评估”只执行滚动，没有真正的视图和选择状态；历史 Run 没有可操作列表，运行中的任务没有明确的结束入口，事件按节点去重后无法还原完整时间线，LaTeX 只有 hash 和 section 元数据，没有正文、差异对比或 PDF 预览，页面还混有大量英文技术字段。

### 解决方案：

将 Scholar 前端重构为中文学术工作台，拆分为“新建任务、运行历史、任务详情、论文稿件、证据与引用、审批中心、运行评估、系统状态”八个真实视图。运行历史点击后会加载对应 snapshot、事件、证据、项目和评估；任务运行时通过 SSE 加完整事件时间线并轮询状态；新增 `/api/scholar/runs/{run_id}/stop` 明确结束入口，沿用持久化的 cooperative cancellation；新增稿件读取接口，复用 `ManuscriptSynchronizer` 的安全路径检查，返回 section 正文、字符/行数、版本、构建状态和 PDF 预览地址；前端增加流程图、事件详情、Section 文件树、LaTeX 原文、原文/提议双栏差异、证据链和真实评估指标。没有 root `.tex` 的研究型项目会显示可读的准备提示，不再阻塞整个任务详情页。

### 最终效果：

前端点击导航会切换真实工作视图；历史 Run 可以逐条打开并查看完整 cursor 事件；运行中的任务可以发出“结束运行”请求并显示安全检查点状态；论文 Section 可直接阅读，已有 PDF 可嵌入预览，AI Patch 可在网页中对比并审批；所有主要操作均连接真实 API。`npm --prefix web run build` 通过，Scholar Console、Patch API 和稿件/PDF 接口测试通过（`13 passed`），完整 Python 测试为 `377 passed`。Docker API、Worker、前端已重建，`http://127.0.0.1:8001/scholar` 与 `https://agent.heuhcy2026.top/scholar` 均已返回新版前端资源；线上真实 API 的 `/manuscript`、`/evaluation`、SSE 和 `/stop` 入口均已探测通过。

## 记录 013：Introduction 引用不足 15 篇不同论文

### 问题：

Introduction 生成结果可能只有 3 篇引用，无法满足学术论文引言至少引用 15 篇不同论文的要求；如果按 Evidence、Chunk 或 BibKey 数量统计，还会把同一篇论文重复计算，或让重复 BibKey 错误满足门槛。

### 解决方案：

将 Introduction 的 Paper-Level 召回预算提升到至少 20 篇，Section 检索提升到至少 30 条，Evidence 预算提升到至少 30 条，并启用按 `paper_id` 补齐的二阶段覆盖。新增 Citation Coverage 合同：只统计经过验证且已经能够绑定到 Citation Store 的不同论文；同一论文的多个 Chunk 只计 1 篇；重复 BibKey 指向多篇论文时判定为歧义；不足 15 篇时在 Writer 前失败，或在 Draft 最终校验失败，均不注册 DraftPatch。CrewAI/Worker/API 保留 `INSUFFICIENT_INTRODUCTION_SOURCES` 与 `INSUFFICIENT_INTRODUCTION_CITATIONS` 错误码和覆盖统计。

### 最终效果：

3 篇论文会返回“还缺 12 篇”并停止在 Patch 生命周期之前；15 篇独立且可绑定引用的论文可以生成待审批 Patch；重复 paper identity 或重复 BibKey 不会被伪造为 15 篇。新增覆盖回归测试验证了上述场景；CrewAI 失败结果会保留 `3 / 15` 覆盖诊断供前端展示。完整 Python 测试为 `385 passed`，前端构建和 `compileall` 均通过。
## 记录 014：论文稿件页面缺少 Overleaf 式 LaTeX 可视化

### 问题：

论文稿件页面原来把 LaTeX 源码、PDF 和 AI 修改差异纵向堆叠，源码没有行号和语法高亮，编译状态与诊断信息也没有独立展示，整体无法像 Overleaf 一样同时阅读源码和查看 PDF。

### 解决方案：

将前端稿件视图重构为三栏 Overleaf 工作区：左侧展示 root.tex 与所有可达 Section 的文件树，中间使用只读 LaTeX 源码查看器展示行号、命令、注释、括号和数学符号高亮，右侧嵌入项目 PDF；在工作区下方读取现有 `latest_build`，展示 Build ID、编译状态、时间、错误、警告、诊断定位和日志。保留 AI Patch 双栏差异与人工接受/拒绝操作，前端不直接写入 `.tex` 文件。

### 最终效果：

进入“论文稿件”即可按“文件 → 源码 → PDF → 编译日志 → Patch 审批”的顺序审阅论文；源码和 PDF 同屏展示，移动端自动降级为文件树、源码、PDF 的纵向布局。前端 `npm --prefix web run build` 通过，Scholar Console 与 Web API 回归测试为 `22 passed`。

## 记录 015：稿件页面只能查看编译结果，不能发起编译请求

### 问题：

Overleaf 式稿件页面虽然可以展示已有 PDF 和 BuildResult，但用户还需要离开页面到 VS Code 才能发起一次 LaTeX Workshop 编译，页面操作链不完整。

### 解决方案：

复用现有只负责登记构建请求的 Build Bridge，在前端增加“请求编译”按钮和 `scholarApi.requestBuild` 调用；请求成功后立即显示 `BUILD_TRIGGERED` 状态，提示等待 VS Code LaTeX Workshop 回报，不由网页直接调用 TeX 编译器。

### 最终效果：

用户可以在“论文稿件”页面发起编译、等待回报、刷新稿件并查看 PDF/诊断日志；构建仍由既有 VS Code Bridge 执行，AI 仍不能绕过人工审批写入论文。前端构建通过，稿件、Build Bridge 和 Patch API 回归测试为 `14 passed`。

## 记录 016：Docker Desktop 显示运行但 Docker Engine 返回 500

### 问题：

执行 `docker compose up -d --build frontend scholar-proxy reverse-proxy` 时，Docker CLI 在访问 `/Users/hanchunyang/.docker/run/docker.sock/_ping` 阶段收到 `500 Internal Server Error`。Docker context 虽然是 `desktop-linux` 且 Docker Desktop 状态显示 running，但 `docker version` 只能返回 Client，Server 无法响应。

### 解决方案：

通过 Docker Desktop VM 控制台日志确认根因是内部 Linux VM 文件系统未就绪：日志出现 `still waiting for fs to be ready after 4m50s`、`context deadline exceeded` 和 services namespace 超时。应先重启 Docker Desktop Engine，等待 `docker version` 同时出现 Client/Server，再重新执行 Compose；不执行 Factory Reset、清理卷或 `docker system prune`，避免破坏项目数据。

进一步检查发现 Docker Desktop 进程来自 macOS 的 AppTranslocation 临时路径，而标准安装路径 `/Applications/Docker.app` 存在。应退出当前实例后从标准安装路径重新启动，以排除应用隔离路径造成的权限或 VM 恢复问题。

### 最终效果：

已确认该故障不是 Compose 文件、前端构建或项目代码错误，当前 Compose 尚未进入容器构建阶段，现有容器和数据未被修改。重启 Engine 并通过 Docker API 健康检查后，才可以继续部署新版 Scholar 前端。

## 记录 017：真实科研问题未生成 LaTeX，线上项目没有可编译稿件

### 问题：

使用线上 Scholar 页面对应的真实问题“我现在希望写一篇低轨卫星课题研究综述”进行端到端验收时，运行 `RUN_565fac55be0ba283` 能够进入 CrewAI、完成 Supervisor 路由和 Research Agent 调用，但最终状态为 `FAILED`，错误事件为 `INSUFFICIENT_EVIDENCE`。结果中没有 `WritingResult`、没有 `DraftPatch`、没有 LaTeX 内容，也没有 PDF。与此同时，`/api/scholar/projects/PROJECT_4ccf97880367b41c/manuscript` 返回项目没有 root `.tex` 文件，`/manuscript/pdf` 返回 400。

### 解决方案：

本次先完成只读真实验收，没有批准 Patch、没有写入论文文件。要得到可验证的 LaTeX 输出，生产项目必须先挂载或创建合法的 root `main.tex` 及可达 Section；Introduction 任务还必须满足至少 15 篇不同论文且可绑定引用的证据门槛。证据不足时应明确展示失败原因和覆盖统计，不能伪造 `.tex` 或 PDF。稿件存在且证据满足门槛后，再继续验证 `DraftPatch → 人工审批 → Build Bridge → PDF` 全链路。

### 最终效果：

已确认 CrewAI 研究链路实际执行，但当前生产数据无法完成“真实问题 → LaTeX → PDF”验收；当前失败属于证据/稿件前置条件未满足，不是前端 PDF 渲染成功。生产项目没有发生文件修改。

## 记录 018：运行评估接口对 RESEARCH、REVIEW 和未完成路由返回 EVALUATION_UNSUPPORTED_TASK

### 问题：

`/api/scholar/runs/{run_id}/evaluation` 只实现了 `SUPPORT_CLAIM`、`WRITE_INTRODUCTION`、`WRITE_CONCLUSION`、`WRITE_ABSTRACT` 四类任务，但运行创建模型还允许 `RESEARCH`、`REVIEW`，而运行中的任务或早期失败任务可能没有 `task_type`。真实探测中，`RESEARCH`、`REVIEW` 以及 `task_type=null` 的 Run 均返回 HTTP 400：`EVALUATION_UNSUPPORTED_TASK`。前端 `refreshRun` 使用 `Promise.all` 同时请求 snapshot、稿件、证据和 evaluation，因此打开这些历史任务时会整体进入错误提示，其他已经成功返回的页面数据也不会提交到界面。

### 解决方案：

评估接口需要区分“该任务没有适用的质量评估”与“评估服务故障”：返回结构化的 `supported=false`、任务类型和原因，或由前端将评估请求降级为可选请求，不应让整个运行详情页失败。对 `RESEARCH`、`REVIEW` 应定义对应评估合同；对 `task_type` 尚未确定的运行，应展示“等待路由/暂无评估”，而不是异常。修复后需要对全量历史 Run、运行中 Run 和失败 Run 做回归验证。

### 最终效果：

当前问题已被线上复现并保留证据：支持的 `SUPPORT_CLAIM` Run 可返回完整评估且通过；`RESEARCH`、`REVIEW` 和未路由 Run 会触发 `EVALUATION_UNSUPPORTED_TASK`，导致历史详情页存在可见故障。此次未修改线上代码。

## 记录 019：历史运行列表包含无法打开的遗留 Run

### 问题：

`/api/scholar/runs` 返回了若干旧的 Run，但对应的 Session/Runtime 投影已不存在；例如 `RUN_0fbf8f31ce5b8ac7`、`RUN_299e3ccf2d92a4d6`、`RUN_5d29a67d621cebd3`、`RUN_a393ffdb9b8c2c25` 在运行列表中出现，但打开 snapshot/evaluation 时返回 Run 不存在。当前项目内还存在部分没有路由信息的早期失败/取消 Run。若前端显示这些记录，用户点击后会得到 404 或 `EVALUATION_UNSUPPORTED_TASK`。

### 解决方案：

运行历史列表必须以可读取的 Console projection 为准，过滤或标记无法关联到当前 Project/Session 的遗留记录；不能只依赖异步 Run Store 列表。对保留的失败/取消 Run，应至少持久化 task type、错误码和终止事件，使详情页可读；对确实无法恢复的历史记录，应显示“历史数据不可用”，而不是把它当作正常运行。

### 最终效果：

线上已复现“列表可见但详情不可用”的数据一致性问题；当前没有删除任何历史记录，也没有修改生产状态。

## 记录 020：修复评估/稿件接口后前端构建与回归问题

### 问题：

修复历史运行和评估接口时发现，前端使用了后端返回的 `detail_available` 字段，但 TypeScript 契约没有声明该字段，导致生产前端构建失败；同时前端已经调用稿件初始化接口，而后端尚未提供该路由。

### 解决方案：

补齐 `POST /api/scholar/projects/{project_id}/manuscript/initialize`，只通过幂等 `ManuscriptSynchronizer.ensure_initialized()` 创建空 LaTeX 骨架，不写 AI 正文、不创建 DraftPatch；补齐前端 Run 类型，历史不可用记录在历史页禁用打开；评估 `supported=false` 时展示结构化原因，不再渲染空指标卡；Introduction 仅在存在实际来源时允许未解析的规划性 claim 继续进入 15 篇引用覆盖校验，空证据仍 fail-closed。

### 最终效果：

`python -m compileall -q app tests` 通过，`npm --prefix web run build` 通过，全量回归为 `388 passed`；稿件初始化、重复初始化不覆盖已有 `main.tex`、`RESEARCH/REVIEW` 无评估合同等回归测试均通过。

## 记录 021：Docker 容器内 LaTeX 稿件不持久化

### 问题：

Compose 原先只挂载 `data` 和 `.scholar`，Scholar 的 `project_root` 是容器内 `/app`。网页初始化或 Agent 创建的 `main.tex`、Section 和 PDF 会落在容器可写层，宿主机编辑器看不到，重建 API/Worker 后也会丢失；切换到新根目录还可能让历史 Project ID 与旧 Run、Citation Binding 脱离。

### 解决方案：

新增 `LEO_PROJECT_ROOT` 配置，Compose API/Worker 使用 `/app/workspace`，并将宿主机项目根目录绑定到该路径；镜像代码仍留在 `/app`。`ScholarProjectStore` 增加显式的 `LEO_PROJECT_ROOT_ALIASES` 迁移逻辑：仅当新路径的项目记录为空且旧路径仍有持久化领域数据时复用旧 Project ID，不跨无关项目猜测。

### 最终效果：

Docker Engine `29.7.2`、Compose 配置校验、API/Worker/Frontend/Scholar Proxy 均正常；线上本地端口初始化接口返回 `200`，宿主机已实际生成 `main.tex`、6 个 Section 和 `references.bib`，重复初始化仍保持 `PROJECT_4ccf97880367b41c`、版本和内容不被覆盖。旧 Citation Registry 数据仍可由同一 Project ID 读取。

## 记录 022：Introduction 多 ResearchNeed 与论文覆盖补齐检索串行

### 问题：

Paper-Level 本身不是逐篇论文串行检索：一次 hierarchical retrieval 会返回最多 20 篇候选；但 Introduction 的背景、已有工作、局限性 3 个独立 ResearchNeed 原先由 `ResearchDelegate` 逐个等待执行，Content Top-K 未覆盖全部 Paper 时的 `paper_id` 补齐查询也逐篇等待。BGE-M3 本地推理因此把一次 Introduction 任务拖成长时间串行执行。

### 解决方案：

将独立 ResearchNeed 交给 `ThreadPoolExecutor`，默认最多 3 路并发；按 Paper-Level rank 并发执行缺失论文的过滤检索，并在收集阶段恢复输入顺序。`ResearchCapabilityService` 增加 1 到 8 的有界检索并发信号量，Evidence Governance 的共享诊断边界单独加锁，BGE-M3/Cross-Encoder 的懒加载增加模型构建锁。生产可通过 `LEO_SCHOLAR_RESEARCH_MAX_CONCURRENCY` 调整并发度。

### 最终效果：

3 个 ResearchNeed 已由并发回归测试验证同时执行，EvidencePack、ClaimPlan 和引用顺序保持确定性；生产 Compose 默认使用 3 路并发，真实 Worker 已观察到并发 BGE-M3 推理。全量 Python 测试 `391 passed`，前端生产构建和 `compileall` 均通过。

## 记录 023：Worker heartbeat 临时异常与旧 Worker 所有权覆盖

### 问题：

真实 Run 曾出现第一次尝试已进入失败/中断、Job 随后重试，但 API 同时投影为旧结果与新的 `RUNNING` 状态。排查发现 heartbeat 线程遇到 SQLite 临时异常会直接退出；旧 Worker 在任务被恢复或接管后仍可能写入 `RETRY_PENDING`/`SUCCEEDED`，覆盖新 Worker 的生命周期状态。用户取消时的合法中间态 `CANCEL_REQUESTED` 也不能被误判为失去所有权。

### 解决方案：

heartbeat 线程对临时 `sqlite3.OperationalError`/`OSError` 自动等待下一周期重试，对状态或 Worker 不匹配则停止写入；任务异常和成功收尾前增加 Worker 所有权检查，旧 Worker 不再覆盖恢复后的状态；取消路径显式允许 `RUNNING → CANCEL_REQUESTED → CANCELLED`。

### 最终效果：

新增临时 SQLite heartbeat、旧 Worker 不覆盖恢复状态、协作取消回归测试；相关回归和全量测试通过。新 Worker 已成功重建并接管现有持久化 Job，未删除 Project、Run、Citation、LaTeX 或向量数据。

## 记录 024：Introduction 的 15 篇文献覆盖检索并发验证

### 问题：

用户需要 Introduction 至少覆盖 15 篇不同论文。如果按论文逐篇等待，BGE-M3 查询和
Content-Level 证据读取会产生明显串行延迟；如果把每篇论文的 Paper-Level 检索也重复
执行，还会违反“先全局 Paper-Level、后限定 Top-K paper_id 的 Level-2”层级合同。

### 解决方案：

Paper-Level 仍只执行一次，生成最多 20 篇候选及稳定 rank。Content-Level 先在这些
`paper_id` 上执行一次混合召回；当结果没有覆盖全部目标论文时，将缺失论文的独立
Level-2 查询一次性提交到有界 `ThreadPoolExecutor`，按 Paper-Level rank 收集结果。
`ResearchNeed` 和论文覆盖池共用 `LEO_SCHOLAR_RESEARCH_MAX_CONCURRENCY`，默认 3 路、
最大 8 路；Qdrant 使用共享服务，BGE-M3/Cross-Encoder 推理保持线程安全。这个并发是
检索已有向量，不会触发任何旧论文 Chunk 或 Embedding 重建。

### 最终效果：

新增 15 篇覆盖回归测试，验证 15 个独立论文查询均被提交、同时最多 3 个任务运行、
Paper-Level 不被重复调用，且输出仍按输入论文顺序恢复。当前本地测试为 `393 passed`，
Ruff、compileall、Compose 配置校验通过；API/Worker 已重建并使用共享 Qdrant。
