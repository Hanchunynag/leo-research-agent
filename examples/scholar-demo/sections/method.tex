The method models a stationary receiver and uses pseudorange and Doppler observations from visible LEO satellites. Ephemeris uncertainty is represented explicitly before a bounded state-estimation step. The implementation records each source locator and keeps manuscript facts separate from literature evidence.
